# =====================================================
# Environment Configuration - VORTEX MOTORS
# Descrição: Variáveis de ambiente (copiar para .env)
# =====================================================

# Ambiente (development, production)
NODE_ENV=development

# URL do site
SITE_URL=http://localhost:3000

# API Configuration
API_BASE_URL=http://localhost:3000/api

# WhatsApp Business
WHATSAPP_NUMBER=5511984039699

# Email Configuration (se aplicável)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha

# Database (se aplicável no futuro)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=vortex_motors
DB_USER=root
DB_PASSWORD=

# Logging Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=DEBUG
